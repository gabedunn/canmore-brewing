version: 2
jobs:
  build:
    docker:
    - image: circleci/node:10-stretch-browsers
    environment:
      NODE_ENV: 'production'
    steps:
    - checkout
    - run: |
        yarn
        yarn lint
        yarn build